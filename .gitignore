*.swp
*.swo
.DS_Store
tags

# nvim runtime log — written by `nvim --headless` smoke runs in this repo,
# environment-specific, not part of the release artifact.
nvim.log
# auto-agents workspace mailbox (v0.2.30+)
.auto-agents/
# PR description bodies, written for `gh pr create --body-file`. They are
# working notes for a single branch, not part of the release artifact — and
# `tests/run-all.sh` stages the worktree's own files (see the KB todo
# 2026-09-02-auto-finder-a-suite-in-testsrun-allsh-stages-the-plugins-own-worktree-files),
# so an untracked-by-habit directory got committed once. Ignored, not remembered.
PRs/
